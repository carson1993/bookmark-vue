# 浏览器扩展安装指南

## 前提条件

- Chrome 浏览器（推荐最新版本）
- 或 Edge 浏览器
- 或其他基于 Chromium 的浏览器
- Node.js ^20.19.0 或 >=22.12.0

## 安装步骤

### 1. 安装依赖并构建

```bash
cd bookmark-app
npm install
npm run build
```

### 2. 打开浏览器扩展管理页面

- Chrome: `chrome://extensions/`
- Edge: `edge://extensions/`

### 3. 启用开发者模式

在右上角找到"开发者模式"开关，将其切换为"开启"状态。

### 4. 加载扩展

- 点击"加载已解压的扩展程序"按钮
- 选择 `bookmark-app/dist` 目录
- 点击"选择"按钮

### 5. 验证安装

扩展应该出现在扩展列表中，打开新标签页测试功能。

## 验证安装

### 检查扩展状态

安装后，在扩展管理页面应看到：

```
十万伏特导航
版本: 1.1.1
权限: 书签、标签页、favicon
```

### 测试功能

1. **打开新标签页** - 新建标签页，显示皮卡丘主题的导航页面
2. **检查书签** - 页面自动加载浏览器书签，按分类显示在侧边栏
3. **测试搜索** - 输入中文/英文/拼音/首字母均可实时过滤（如输入 "aly" 搜到 "阿里云"）
4. **测试拖拽** - 拖拽书签卡片到侧边栏分类可移动书签
5. **测试测速** - 点击右上角 WiFi 图标，检测当前分类书签的连通性
6. **测试主题** - 点击右上角月亮/太阳图标切换深色/浅色模式
7. **测试弹窗** - 点击浏览器工具栏的扩展图标，显示弹窗界面

## 常见问题

### Q1: 扩展无法加载？

**A:** 检查以下几点：
- 先执行了 `npm install` 安装依赖
- `manifest.json` 文件存在于 `dist` 目录
- 所有引用的文件路径正确
- 控制台是否有错误信息

### Q2: 书签没有显示？

**A:**
- 检查浏览器是否有书签
- 确认扩展有"书签"权限
- 刷新页面重试

### Q3: 书签图标显示空白？

**A:**
- 系统内置四级降级加载（本地缓存 → Chrome 缓存 → 网站直取 → Google CDN → 占位图）
- 首次成功加载后会缓存到 localStorage，后续打开无需网络请求
- 如果持续空白，检查网络连接

### Q4: 搜索不支持拼音？

**A:**
- 已支持拼音全拼和首字母搜索
- 例如输入 "aliyun" 或 "aly" 可搜到 "阿里云"
- 确保书签标题中包含中文才会生成拼音索引

### Q5: 如何更新扩展？

**A:**
1. 修改代码后重新构建：`npm run build`
2. 在扩展管理页面点击"刷新"图标
3. 或者移除扩展后重新加载

### Q6: 如何调试？

**A:**
1. 新标签页右键 → 检查，查看控制台日志
2. 在扩展管理页面点击"检查视图" → "Service Worker"，查看后台日志
3. 检查控制台是否有错误

## 安装验证清单

- [ ] 扩展出现在扩展列表中
- [ ] 没有错误提示
- [ ] 新标签页显示皮卡丘主题界面
- [ ] 书签正常加载
- [ ] 搜索功能正常（含拼音）
- [ ] 拖拽移动功能正常
- [ ] 链接测速功能正常
- [ ] 弹窗功能正常
- [ ] 深色/浅色主题切换正常
- [ ] 所有链接可点击

## 首次使用

1. 打开新标签页
2. 浏览侧边栏的书签分类
3. 尝试拼音搜索（如输入 "aliyun" 搜 "阿里云"）
4. 拖拽书签到其他分类
5. 点击测速按钮检查链接连通性

## 获取帮助

如果遇到问题：
1. 查看浏览器控制台错误
2. 检查扩展权限设置
3. 确认所有文件完整
4. 重新执行 `npm install && npm run build` 并重新加载

---

**版本**: 1.1.1  
**作者**: carson1993  
**最后更新**: 2026-06-15